<h1><%= post.title %></h1>
<p><%= post.content %></p>
<p>작성일: <%= post.created_at.toLocaleString() %></p>
<h2>댓글</h2>
<ul>
  <% comments.forEach(comment => { %>
    <li>
      <strong><%= comment.User.username %></strong>: <%= comment.content %>
    </li>
  <% }) %>
</ul>

<form method="POST" action="/community/<%= post.community_post_id %>/comment">
  <textarea name="content" placeholder="댓글을 입력하세요" required></textarea><br>
  <button type="submit">댓글 작성</button>
</form>

<a href="/community/<%= post.community_post_id %>/edit">수정</a>
<form action="/community/<%= post.community_post_id %>/delete" method="POST" style="display:inline;">
  <button type="submit">삭제</button>
</form>
<a href="/community">목록으로</a>
